name: Bilibili Data Pipeline Auto-Update

on:
  schedule:
    - cron: '15 * * * *'
  workflow_dispatch:

# 1. 授予写入权限，以便 bot 可以推送提交
permissions:
  contents: write

jobs:
  scrape_and_clean:
    runs-on: ubuntu-latest
    
    # 2. 屏蔽 Node.js 20 弃用警告并强制使用 Node 24
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # 避免浅克隆导致的冲突问题
          
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache pip
        uses: actions/cache@v4 # 升级到 v4 版本
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Scraper
        run: python scraper.py

      - name: Run Data Cleaning
        run: python data_cleaning.py

      - name: Commit and Push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # 拉取远程更新，防止冲突
          git pull --rebase origin ${{ github.ref_name }}
          
          git add data/
          
          if git diff-index --quiet HEAD --; then
            echo "没有检测到数据变动，跳过推送。"
          else
            timestamp=$(date '+%Y-%m-%d %H:%M:%S')
            git commit -m "🤖 Auto-update Bilibili ranking data - $timestamp [skip ci]"
            git push
          fi
